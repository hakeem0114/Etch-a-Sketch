let container = document.getElementById("grid");
let cell = document.createElement("div"); //Creates an empty div for new cells

//cell.textContent = i;
//container.appendChild(cell); //Adds a new div to the container

function main_grid(){
    for(let i=0;i < 17;i++){
        const cell = document.createElement("div");
        div.style.cssText = "border: 2px solid blue; height: 20px; width:20px"
        container.appendChild(div)
    }
}
main_grid();